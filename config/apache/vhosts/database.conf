<VirtualHost *:80>

    ServerName database.loc
    ServerAlias www.database.loc
    ServerAdmin admin.localhost

    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI}

</VirtualHost>


<IfModule mod_ssl.c>
    <VirtualHost *:443>

        # START Virtual Host Information Directives #
        ServerName database.loc
        ServerAlias www.database.loc
        ServerAdmin admin.localhost
        # END Virtual Host Information Directives #

        # START SSL Directives #
        SSLEngine on

        SSLCertificateFile /etc/apache2/server.crt
        SSLCertificateKeyFile /etc/apache2/server.key

        #################################
        #START Application Configuration#
        #################################
        DocumentRoot /srv/www/database-admin
        DirectoryIndex index.php

        <Directory "/srv/www/database-admin">
            Options Indexes FollowSymLinks Includes ExecCGI
            AllowOverride All
            Require all granted
        </Directory>

    </VirtualHost>
</IfModule>